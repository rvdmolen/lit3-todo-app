
import { css } from 'lit';

export const globalToDoAppStyles = [
  css`

    h2 {
      height: 3rem;
      color: var(--dark);
      text-align: center;
    }


    .uppercase {
      text-transform: uppercase;
    }

    .letter-spacing-3 {
      letter-spacing: 3px;
    }

    .text-white {
      color: var(--white);
    }

    .text-dark {
      color: var(--dark);
    }

    .text-grey-dark {
      color: var(--grey-dark);
    }

    .text-purple-dark {
      color: var(--purple-dark);
    }
  `,
];
